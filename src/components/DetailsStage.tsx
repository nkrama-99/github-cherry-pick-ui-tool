import { Typography, Link, Container, Divider } from "@mui/material";
import { FC } from "react";

const DetailsStage: FC = () => {
  return (
    <Container>
      <Typography component="h1" variant="h4" align="center">
        Stage 0
      </Typography>
      <Divider />
    </Container>
  );
};

export default DetailsStage;
